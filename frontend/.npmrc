# 这个项目单独用国内源，不动你的全局 npm 配置。
#
# 为什么需要：这台机器上有 Clash 代理（HTTP_PROXY=127.0.0.1:7897），
# 而代理在大文件传输上不稳定 —— Docker 拉 MySQL 镜像时就被掐断过好几次
# （小镜像能过、150MB 的大层卡死）。npm install 要拉两百多个包，
# 走代理的话很容易卡在某个包上。
#
# npmmirror 是国内直连，不需要代理，速度也快得多。
registry=https://registry.npmmirror.com

# 装不上时把这两行打开看详细日志
# loglevel=verbose
# fetch-timeout=120000
